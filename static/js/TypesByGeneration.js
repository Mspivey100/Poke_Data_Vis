function unpack(rows, index) {
    return rows.map(function(row){
        return rows.map(row) {
            return row[index];
        }
    });
}

d3.json('/graphcount', function (data){
var trace = {
    x: data.map(row => row.generation)
    y:data.map
}
});